{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "air",
    "build": "go build -o bin/server ./cmd/server",
    "lint": "golangci-lint run ./...",
    "test": "go test ./...",
    "clean": "rm -rf bin tmp"
  }
}
