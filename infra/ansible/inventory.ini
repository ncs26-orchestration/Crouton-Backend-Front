[app]
# Host IP comes from: cd infra/terraform && terraform output -raw vm_ip
# The project enforces OS Login, so we connect as the OS Login POSIX user with
# the gcloud-registered key (~/.ssh/google_compute_engine), not a custom user.
35.241.240.210 ansible_user=walidbechar_pro_gmail_com ansible_ssh_private_key_file=~/.ssh/google_compute_engine ansible_python_interpreter=/usr/bin/python3

[app:vars]
ansible_ssh_common_args=-o StrictHostKeyChecking=accept-new -o IdentitiesOnly=yes
